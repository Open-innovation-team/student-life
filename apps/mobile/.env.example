# Copier en `.env`. URL de base de l'API NestJS, exposee au bundle Expo.
# IMPORTANT : sur un device physique (Expo Go), "localhost" pointe vers le
# telephone. Remplacer par l'IP LAN de la machine de dev, ex : http://192.168.1.20:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
